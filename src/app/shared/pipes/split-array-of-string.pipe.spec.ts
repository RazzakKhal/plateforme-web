import { SplitArrayOfStringPipe } from './split-array-of-string.pipe';

describe('SplitArrayOfStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitArrayOfStringPipe();
    expect(pipe).toBeTruthy();
  });
});
